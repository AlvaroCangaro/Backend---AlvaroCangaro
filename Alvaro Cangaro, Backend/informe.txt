Los comandos que se ejecutaron estan en comandos.txt

Se realizo un test con artillery, el resultado de artillery esta en el archivo result.txt, y el resultado de node profiler esta en artilleryTestResults.

Se realizo un test con autocannon a traves del codigo en el archivo autocannonBenchmark, el resultado esta en resultadoAutocannon.png.

Se reviso el perfilamiento del servidor con el modo inspector de node.

Se realido un diagrama de flama con 0x, emulando carga con autocannon en el archivo autocannonBenchmark. El resultado esta en la carpeta 1392.0x.

El servidor responde mejor en modo cluster y con la menor cantidad de procesos bloqueantes.